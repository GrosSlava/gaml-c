// operator while



func void main()
{   
    /*
        while is similar to c/c++, but we don't have version without brackets
    */
    int32 a = 999;
    while( a > 0 )
    {
        // while namespace

        // break - exit while
        // continue - go to the next iteration

        a--;
    }
}