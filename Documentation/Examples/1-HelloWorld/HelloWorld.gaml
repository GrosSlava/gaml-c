// Copyright may be here.

import std.stdio; // import module with read/write functions



func main
{
    std.stdio::print("Hello world!");

    /*
        Waiting for user input to prevent the console from closing.
    */
    std.stdio::getc();
}